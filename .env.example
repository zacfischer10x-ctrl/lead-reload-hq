# Stripe (paste live or test keys in Netlify UI — never commit real values)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Invite-only admin (/admin/) — usernames: dan | zac
ADMIN_DAN_PASSWORD=change-me-dan-long-random
ADMIN_ZAC_PASSWORD=change-me-zac-long-random
# Optional legacy fallback (logs in as dan)
ADMIN_PASSWORD=optional-legacy-shared-password
ADMIN_SESSION_SECRET=change-me-to-another-long-random-hex

# Optional — defaults to the request origin at runtime
SITE_URL=https://lead-reload-hq.netlify.app
